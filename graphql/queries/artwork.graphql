query artwork($artworkId: ID!) {
    artwork(artworkID: $artworkId) {
        artwork {
            id
            title
            description
            imageUrls
            createdAt
            likesCount
            isLikedByLoggedInUser
            uploader {
                id
                username
                avatarUrl
            }
            comments {
                id
                comment
                parentComment {
                    id
                }
                likesCount
                isLikedByLoggedInUser
                commenter {
                    id
                    username
                    avatarUrl
                }
            }
        }
        errors {
            message
        }
    }
}
