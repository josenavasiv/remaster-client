query notifications($skip: Int, $take: Int) {
    notifications(skip: $skip, take: $take) {
        notifications {
            ...Notification
        }
        hasMore
        errors {
            message
        }
    }
}
