mutation artworkDelete($artworkId: ID!) {
    artworkDelete(artworkID: $artworkId) {
        artwork {
            id
            title
        }
        errors {
            message
        }
    }
}
