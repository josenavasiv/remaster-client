mutation commentCreate($artworkId: ID!, $comment: String!) {
    commentCreate(artworkID: $artworkId, comment: $comment) {
        comment {
            ...Comment
        }
        errors {
            message
        }
    }
}
