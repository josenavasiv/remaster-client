mutation commentUpdate($commentId: ID!, $comment: String!) {
    commentUpdate(commentID: $commentId, comment: $comment) {
        comment {
            id
            comment
        }
        errors {
            message
        }
    }
}
