mutation commentDelete($commentId: ID!) {
    commentDelete(commentID: $commentId) {
        comment {
            id
            comment
        }
        errors {
            message
        }
    }
}
