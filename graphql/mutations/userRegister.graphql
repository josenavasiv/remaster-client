mutation userRegister($username: String!, $email: String!, $password: String!, $avatarUrl: String) {
    userRegister(username: $username, email: $email, password: $password, avatarUrl: $avatarUrl) {
        user {
            id
            username
            avatarUrl
            email
        }
        errors {
            message
        }
    }
}
